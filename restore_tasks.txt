// Для восстановления обычного режима замените в src/services/api.js:

getTasks: async () => {
  // ВРЕМЕННО: возвращаем пустой массив для тестирования EmptyState
  return [];
  
  // try {
  //   const response = await api.get("/api/kanban");
  //   return response.data.tasks || [];
  // } catch (error) {
  //   throw new Error(
  //     error.response?.data?.error || "Ошибка при загрузке задач"
  //   );
  // }
},

// НА ОБЫЧНЫЙ КОД:

getTasks: async () => {
  try {
    const response = await api.get("/api/kanban");
    return response.data.tasks || [];
  } catch (error) {
    throw new Error(
      error.response?.data?.error || "Ошибка при загрузке задач"
    );
  }
}, 